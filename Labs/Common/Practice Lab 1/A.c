#include <stdio.h>

typedef long long ll;

void solve()
{
    int m;
    int n;
    int q;
    scanf("%d %d %d", &n, &m, &q);
    ll area = (n - 1) * (m - 1);
    for (int i = 0; i < q; i++)
    {
    }
}

int main()
{
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        solve();
    }
}